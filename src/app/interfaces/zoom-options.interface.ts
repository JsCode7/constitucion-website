export interface ZoomOptions {
    zoomMode: 'hover-freeze' | 'click' | 'hover' | 'toggle'; // Puedes ajustar los valores según la documentación
    scrollZoom: boolean;
    lensMode: boolean;
    lensWidth: number;
    lensHeight: number;
  }
  